# Handoff — 2026-09-20

Worktree: `C:/Projects/msp-render-pipeline-scene-prep`, branch
`codex/studiomark-scene-prep` (single upstream: GitHub `origin`). Supersedes
`HANDOFF-2026-09-17.md`. Everything through `412f832` is **pushed** and the
tree is clean. Suite **134 tests OK** with exit 0
(`./.venv/Scripts/python.exe -m unittest discover -s tests`, absolute
interpreter path; check `${PIPESTATUS[0]}` from Git Bash — a PowerShell
`2>&1` merge corrupts the exit code). The short version: **the stall broke —
Mark ruled all rendering onto Modal, the T-V1 brief landed, the cloud preview
dispatcher works, touch-up batches 1 and 2 are DONE and KEPT, and the first
motion artifact exists. Next: batch 3 (real glossy floor), then batch 4
(metal imperfections, owner-gated), then the T-V2 sequencer.**

---

## Owner rulings, 2026-09-20 — verbatim

1. *"**no videos locally. Always in Modal. There is $30 per month free usage
   and it's way faster. This laptop can't handle it well.**"* All render work
   — video frames and previews — runs on Modal. Local machine does dispatch,
   compositing, and encoding only. Ceiling: USD 30/month, recorded in
   `docs/cloud-smoke.md` ("2026-09-20 authorization") with per-run estimates
   in each dispatch's `request.json`. This supersedes the video brief's
   local-first T-V2 scope; its §4.3 batching analysis is now the sequencer's
   core constraint.
2. *"**lets keep them. move on to batch 2**"* — batch-1 touch-ups (DOF f/3.2,
   contact shadow 0.35, deepened sweep) are the standing look. Batch 2
   (vignette/bloom/grain) delivered and shown; Mark then approved moving on:
   *"**All right that sounds good man**"* on the batch-2 report.
3. Touch-up items 1, 2, 3, 6 are DONE and kept. Remaining: **batch 3 = real
   glossy floor** (this handoff's next action), **batch 4 = metal
   imperfections** — owner-gated by the standing material rule: material
   changes mean a new source `.blend` version and an acceptance round
   (the latch lesson; do not put material fixes in job manifests).

## Measured facts this session (all in docs/cloud-smoke.md)

- **Warm L4 marginal render: 10.05–10.2 s/frame** at production settings
  (1800×1250, 96 spp, OIDN, AgX). Cold container ≈ 188–236 s (OptiX kernel
  compile; the DOF variant adds ~52 s once). A 300-frame 1.2°/frame loop
  ≈ **USD 1.30 and ~55 min** in 30-frame batches on one L4.
- **DOF costs +1.5% warm** (10.2 vs 10.05 s). Everything else in batches 1–2
  is composite-side and free.
- The 2026-09-17 `render-report.json` bytes-build_hash fix is **proven on
  real cloud Blender** (valid report, `"build": "b70da489d7f4"`).
- 30-frame orbit probe: all gates passed — per-frame product integrity,
  grain determinism (byte-identical rerun), no flicker outliers, decode-back
  PSNR 39.6–40.4 dB vs declared 35 dB floor. §7.4 scalars at 12°/frame are
  recorded as measurements, NOT gates; declare their thresholds in the T-V2
  manifest at 1.2°/frame, never retrofitted.

## The machines that now exist

- `scripts/cloud_job_render.py` — dispatches any job manifest's azimuths to
  Modal (pinned Blender 5.1.1 archive, L4, one Blender process per frame,
  sequential remote calls so a failed frame loses only itself), downloads
  hashed artifacts, composites locally via `MSPCompositor`. `--set
  dotted.path=value` applies taste overrides to a copy (unknown paths and
  missing local inputs fail at preflight — before any billable call;
  overrides recorded in `request.json`). **Modal 1.5.1 trap:** it pickles
  imported functions by reference — never call local modules inside the
  container function; evaluate evidence locally from returned samples.
- `composite_worker.py` lens pass — `apply_lens_pass(image, vignette, bloom,
  grain, frame_index)`, pure; grain seeded by frame_index alone (antiflicker
  by construction). Runs AFTER the T05 gates; all-zero params leave accepted
  composites byte-identical. Current kept look: vignette 0.45, bloom 0.3,
  grain 2.2.
- `scripts/probe_sequence.py` — consumes a run dir, replays its recorded
  overrides, composites every frame through the gates, lenses, proves grain
  determinism, computes §7.4 statistics on PRE-lens pixels, encodes H.264
  with decode-back PSNR. Exit 0 = all declared gates passed.
- `scripts/build_studio_white_plate.py` — `--floor-darkening`/`--vignette`;
  defaults regenerate the accepted plate byte-identically. v2 plate
  (`backgrounds/env_studio-white-v2.png`) is the kept sweep.

## Batch 3 — the exact next task

**Goal:** replace the flat composite backdrop with a real rendered floor the
machine sits on, catching soft reflections — killing the one remaining
artifact both vision reads flagged (the machine-to-backdrop boundary reads
sharper than the machine's own DOF falloff). This also makes DOF fully
believable (there is finally something behind the machine to blur).

Design decisions the fresh session must make (recommendations included):

1. **Where the floor lives:** in-scene, in `render_worker` — a floor plane
   under the machine (light-neutral, low-roughness glossy), following the
   existing `GroundShadowCatcher` pattern (`render_worker.py` setup_lighting
   adds a plane sized `radius*14`; scene-bounds discipline applies — see the
   discarded-probe lesson in the brief §11: bounds mean different things
   before/after dressing). Recommended: manifest-gated `lighting.floor`
   block so stills jobs are untouched; a new preview job variant, not a
   change to `rl300_04`.
2. **film_transparent consequences:** a rendered floor means the beauty
   carries real background; the composite path becomes pass-through (plate
   optional). The T05 product-integrity gate still works (product pixels vs
   render) but the mask/paste machinery idles. Keep the plate-composite
   path the default; floor mode is opt-in per manifest.
3. **Measurement:** one 2-frame probe dispatch (az 42 + 222, same as every
   preview) measures the render-cost delta (estimated +10–30%; table in the
   2026-09-20 conversation, recorded as estimate). Record measured seconds
   in `docs/cloud-smoke.md` per convention.
4. **Look:** floor should catch the softbox reflections and the contact
   shadow naturally (the in-scene shadow catcher already exists); the
   composite `shadow_opacity` may become redundant in floor mode — test
   rather than assume.

Then: **probe with the full kept look** (`--set camera.depth_of_field.f_stop=3.2`,
floor enabled), vision-read the boundary, run `scripts/probe_sequence.py` on
a short orbit, record, commit. Roughly one focused session.

**After batch 3:** batch 4 (metal imperfections — needs Mark's pick of the
imperfection set and an acceptance round), then T-V2 sequencer: wrap
`cloud_job_render` in a resumable 30-frames-per-container loop + ffmpeg per
`probe_sequence`, sequence gates declared at 1.2°/frame, `awaiting_owner_review`
artifact, zero local renders.

## Standing constraints (unchanged)

- **$30/month Modal ceiling**; per-run estimates in `request.json`, measured
  seconds recorded in `docs/cloud-smoke.md` after each run.
- **`cloud_authorized` stays hardcoded false** — human authorization is
  recorded in docs, never a machine gate.
- **No threshold widening without numbers**; no local renders; single
  upstream `origin`; `main` worktree has none of this branch.
- Mark's acceptance requires his explicit words; "looks good" ≠ formal
  acceptance for parity artifacts.
- Material changes → new source version + acceptance round. Camera/scale
  ruling for the excavation-pit job stands (dm 5.6, offset_z 0.85).

## Evidence directories (all gitignored, all local)

- `output/preview-turntable-20260920-run3/` — first cloud preview (base look)
- `output/preview-turntable-batch1-20260920/` — batch-1 look
- `output/preview-turntable-batch2-20260920/` — 30-frame orbit + **first
  motion artifact** `turntable-loop.mp4` + `sequence-report.json`
