# Handoff — 2026-09-23 (batch 3f: Key-only isolation ran; az42 Key-off 2.4253 FAILS 1.8 — the Key-alone hypothesis is DISPROVED; the whole analytic rig matters; the carrier among Fill/Rim/interaction is NOT yet identified)

Worktree: `C:/Projects/msp-render-pipeline-scene-prep`, branch
`codex/studiomark-scene-prep`, **7 local commits ahead of `origin`** (push waits
for Mark's words). Supersedes `HANDOFF-2026-09-22-batch3e.md`. No full suite
ran this round (it contains local render stubs; local renders are banned) —
last full number stands at **239 tests OK** from 3e; the targeted subset
`-m unittest discover -s tests -p test_floor_mode.py -k key_enabled` ran
**9 tests OK** under the project venv this round (architect-verified).

## Short version

Batch 3f built an opt-in `lighting.key_enabled` control (GLM worker, DeepSeek
adversarial implementation review = SHIP-TO-MODAL, diagnostic only), dispatched
the frozen two-frame Modal Key-off probe (**already executed — do not
re-dispatch**), and documented the measured result under a second adversarial
review pass. Result: **az42 pre-lens 2.4252777 (row 553) FAILS the unchanged
1.8 gate** — worse than all-lights 1.9744644; **az222 0.8924637 PASSES its
separate ≤1.0 diagnostic guard**. Removing the Key therefore does **not** clear
the az42 defect; a Key-only fix cannot resolve it. The earlier all-analytic-off
probe still passes az42 (0.8016129), so the **combined** analytic rig is load
-bearing — but Key/Fill/Rim are **not separated**, and the Key cannot be
excluded as a contributor (it may interact). **Do not re-inherit 3e's
Key-attribution** (HANDOFF-3e:23) — 3f disproved it. No permanent rig change,
no threshold change, no owner look acceptance, nothing committed, no push.

## Read first, in order

1. This file.
2. `AGENTS.md` — environment facts (venv Python absolute path,
   `${PIPESTATUS[0]}` trap, Blender not on PATH, local renders banned,
   `output/` gitignored evidence).
3. `docs/rl300-parity.md` — standing owner rulings, verbatim.
4. `docs/cloud-smoke.md` — the new "2026-09-23 batch-3f key-only isolation"
   section (appended, starts after the 3e section) plus the 3e entry above it.
5. `output/batch3f-key-only-results.md` (measured result note) and
   `output/batch3f-key-results-review-glm.md` (fresh-context reviewer, SHIP
   after F1–F4 corrections).

## The measured numbers (pre-lens, `measure_background_step`, unchanged metric)

| metric | 3e all-lights | 3f all-analytic-off | 3f Key-off |
|---|---|---|---|
| az42 full max_step (row) | 1.9744644 (552) | 0.8016129 (3) | **2.4252777 (553) FAIL >1.8** |
| az42 left / right halves | 0.7721 / 3.3585 | 0.8049 / 0.7992 | 0.7652 / **4.1846** |
| az222 full max_step (row) | 0.7901154 (3) | 1.0019073 (3, guard MISS) | 0.8924637 (3) PASS |

Eligible background columns are identical across all three runs (az42 252 left /
319 right of 571; az222 240/326 of 566; symmetric difference 0) — no
mask-column drift. v2 env sha `617e63a7…` identical in all three
(`request.json:15`). **Build caveat:** 3e and 3f-ibl ran worker
`153497382f8c09f7…`, the Key-off run ran `fde78deacaa381d1…`
(`request.json:17` each); the only source delta is the Key block wrapped under
`lighting_spec.get("key_enabled", True)`. No same-build pixel baseline exists,
so cross-run pixel comparisons are a controlled comparison **subject to a
build-invariance assumption**. The gate FAIL is direct on the Key run's own
build and does not need that assumption. Product mean luma (ITU-R 601,
`mask == 255`): az42 133.25 → 70.33; az222 106.42 → 96.93 — the Key-off look is
much darker; it is a diagnostic, not a candidate look.

## What to review (Mark)

Frames: `output/preview-studio-floor-3f-key-20260923/cloud/frame-az{42,222}/`
and `.../frame-az{42,222}/` under `output/preview-studio-floor-3f-ibl-20260923/`
(`composite-prelens.png` pre-lens gate input, `composite-lens.png` shipped
look, `mask.png`; generated by `scripts/probe_sequence.py`, which the
dispatcher does not run). Compare az42 against 3e
(`output/preview-studio-floor-3e-20260922/...`): the step at row ~552 is
unchanged in character and slightly stronger with the Key off.

**Decisions that are yours:**

1. **Fill-off / Rim-off isolation next (recommended).** The carrier is
   unresolved; the discriminating experiment is one-light removal per light,
   mirroring this probe. That needs `lighting.fill_enabled` /
   `lighting.rim_enabled` opt-in controls (code + tests + review), then a
   two-frame (az42 + az222) Modal diagnostic **per light** — each dispatch
   ≈ USD 0.197 estimate / ≈ USD 0.08 rate-derived (not billed). Pair roles for
   that task: GLM worker / DeepSeek reviewer.
2. **Or look at az42 as-is.** 2.4253 with the Key off vs 1.9745 all-lights; if
   your eye accepts the all-lights frame, the chain can close and push.
3. **Or revert the v2 env manifest flip** (one line in
   `jobs/rl300_05_studio-floor.json` back to `env_studio-softbox.png`) — the
   3e v2 open items below stay unresolved either way.

**Owner sign-offs still open (unchanged from 3e — none cleared by 3f):**

- LDR headroom: constraint-11 band-local reading violated as measured
  (pre-clip in-band max 1.29997; 13,541 band pixels newly clipped).
- "Worst azimuth step ≤ half of v1" bar missed (ratios 0.529 / 0.676).
- New elevational edge: worst smoothed gradient 18.96 → 33.19 byte/deg at
  az ~259 / el ~28.4 — outside both probe frames but imaged by an orbit
  azimuth; must be smoothed before any full-orbit claim.
- Full-orbit claim and owner look acceptance (2-frame probes support neither).

## What the next agent must know

- **Probes already paid for — do not re-dispatch.** Modal apps: Key-off
  `ap-CUlinGZ4CirXYPhFzVRWnY` (`output/batch3f-key-modal.log:3,20-24`, both
  frames `floor_composite … success`); all-analytic-off app id as recorded in
  the cloud-smoke 3f section. Both dispatches: estimate USD 0.197 each;
  measured frame seconds 232.933 s (IBL) and 245.940 s (Key) → **USD 0.072 /
  0.076 RATE-DERIVED at 0.00030992 USD/s, no invoice queried**.
- **Implementation state (dirty, uncommitted):** `render_worker.py` (Key block
  gated, ≈line 437), `docs/job_manifest.schema.json` (optional boolean
  `lighting.key_enabled`, default true), `scripts/cloud_job_render.py`
  (`SCHEMA_OPTIONAL_OVERRIDE_PATHS = {"lighting.key_enabled"}`, line 116;
  creates the path only when an explicit `--set` names it), `tests/test_floor_mode.py`
  (9 focused tests). **Lesson from this round:** the first implementation
  pre-seeded `key_enabled=true` into EVERY generated frame manifest (silently
  changing hashes of unrelated accepted jobs); the architect caught it and the
  correction landed before any cloud use. Never inject defaults into unrelated
  jobs — only an explicit operator override may create an optional path.
- **Pair protocol (owner instruction 2026-09-22):** worker + adversarial
  reviewer, never same family, roles flip each task. 3e = DeepSeek worker / GLM
  reviewer; 3f implementation = GLM worker / DeepSeek reviewer; 3f results +
  this handoff = DeepSeek producer / GLM reviewer (plus a final GLM check).
  Next task flips to **GLM worker / DeepSeek reviewer**. Plain dispatches (no
  workflow harness); seats write files, return ≤ 400 words; reviewer gets
  CONTRACT + ARTIFACT/DIFF only; architect re-runs every verification and
  reproduces each claimed defect before dispatching a fix.
- **seatwrap trap:** its re-run of the spec-declared VERIFICATION uses
  Windows `cmd.exe`, which rejects `./.venv/...` ("'.' is not recognized").
  Pass `--verify-cmd` with a cmd-compatible command
  (`.venv\Scripts\python.exe -m unittest …`) or make the spec's VERIFICATION a
  single bare line; otherwise the ledger shows a false `rerun_failed` even
  when the architect's direct venv run passed.
- **Model-attribution recipe:** `prove-served-model.sh --lane ocx --expect`
  wants the **resolved** id (`deepseek-flash`, not the `deepseek/…` request
  form) and a tight `--since`; the proxy log window can contain other
  sessions' traffic (a `gpt-6-sol` row produced a MISMATCH exit 3). Filter
  `ocx logs --json` by dispatch window + `requestedModel` and expect
  `resolvedModel: deepseek-flash`.
- **Review artifacts on file:** `output/batch3f-rig-derivation-review-deepseek.md`,
  `output/batch3f-key-isolation-review-deepseek.md`,
  `output/batch3f-key-results-review-glm.md` (F1–F4, all corrected and
  re-verified). Frozen contracts: `batch3f-key-only-probe-contract.md`,
  `batch3f-rig-diagnostic-contract.md`. All in `output/` (gitignored).
- **Concurrent workstream:** untracked `scripts/*webexport*.py` (airway/corridor
  probes, GLB export, render script) plus `.codex/`, `.zcodeignore`,
  `docs/agent-tooling.md` and the modified `AGENTS.md` are NOT this round's —
  do not touch, do not commit, do not clean up. Commit with explicit paths only.
- **Deferred cheap hardening (still open, 3e GLM finding 7):** refuse to
  overwrite `ACCEPTED_OUTPUT` in `scripts/build_studio_softbox_env.py` unless
  the existing sha matches.
- `probe_sequence.py --run-dir <dir> --expect-frames 2 --no-encode` is the
  2-frame probe invocation (no MP4).

## Standing constraints (unchanged)

- All rendering on Modal, never local (2026-09-20 ruling); USD 30/month
  ceiling; estimates in `request.json` before dispatch, measured seconds after,
  failures recorded too; `cloud_authorized` stays hardcoded false.
- No threshold widened without new numbers — the 1.8 gate and
  `measure_background_step` are untouched; 3f's Key-off FAIL is reported, not
  rationalized. "Looks good" is not owner acceptance; diagnostics are not
  candidate looks.
- Material/asset changes need a new source asset + acceptance round (ruling 2).
- Single upstream `origin`; the `main` worktree at
  `C:/Projects/msp-render-pipeline` has none of this branch.
- Python: `./.venv/Scripts/python.exe`. Blender at
  `C:/Program Files/Blender Foundation/Blender 5.1/blender.exe` (headless
  `--background --python` only, never a render). ffmpeg on PATH. From Git Bash,
  check `${PIPESTATUS[0]}` — a PowerShell `2>&1` merge corrupts the exit code.
