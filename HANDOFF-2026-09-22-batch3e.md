# Handoff — 2026-09-22 (batch 3e: env horizon round ran; az42 gate FAILS at 1.9745 — the pre-declared criterion DISCONFIRMS the env-only mechanism; the analytic rig is the evidence-backed next target)

Worktree: `C:/Projects/msp-render-pipeline-scene-prep`, branch
`codex/studiomark-scene-prep` (single upstream GitHub `origin`). Supersedes
`HANDOFF-2026-09-22-batch3d.md`. Suite **239 tests OK, exit 0** (venv).
Local commits only, like the rest of the batch-3 chain: push waits for
Mark's words.

## Short version

Batch 3e executed the declared env horizon-band round end to end under the
owner's pair protocol (DeepSeek worker, GLM adversarial reviewer,
cross-family; roles flip on the next task). The generator gained a
`horizon-lift` profile composing on the accepted map's BYTES (out-of-band
rows array_equal; accepted profile byte-identity re-verified under venv),
new asset `backgrounds/env_studio-softbox-v2.png`, manifest flip of
`lighting.hdri_path` (that key only). One 2-frame Modal probe (USD 0.197
estimate, **USD 0.073 rate-derived measured**): every structural gate
green, az222 0.7901 unchanged-PASS — **az42 1.9745 > 1.8 FAIL**. The lift
moved az42 2.1536 -> 1.9745 (-8.3%), which crosses the pre-declared 1.5
falsification line: **an env-only fix cannot close the az42 defect.** The
derivation seat's contested measurement — the defect lives in the az42
right-background segment on byte-identical env columns — is confirmed: the
world-fixed analytic rig (Key_Softbox at world az ~45) carries the defect.
**The rig round is next**, and per the owner's protocol it runs with roles
FLIPPED: GLM worker, DeepSeek reviewer.

## Read first, in order

1. This file.
2. `AGENTS.md` — environment facts (venv Python absolute path,
   `${PIPESTATUS[0]}` trap, Blender not on PATH, local renders banned,
   `output/` gitignored evidence).
3. `docs/rl300-parity.md` — standing owner rulings, verbatim.
4. `docs/cloud-smoke.md` "2026-09-22 batch-3e" — measured run record.

## What to review (Mark)

Frames: `output/preview-studio-floor-3e-20260922/cloud/frame-az42/` and
`.../frame-az222/` (`composite-prelens.png` pre-lens;
`composite-lens.png` shipped look — generated by `scripts/probe_sequence.py`,
the stage the dispatcher does not run). Compare az42 against 3d
(`output/preview-studio-floor-3d-20260922/...`): the upper background is
brighter and softer, the step at row ~552 remains (1.97 vs 2.15).

**Decisions that are yours:**

1. **Rig round next (recommended):** target the analytic rig
   (`setup_lighting`'s key/fill, world-fixed) so the az42 right-background
   segment stops carrying a hard step. Asset/acceptance round under
   ruling 2. Runs with flipped pair roles (GLM worker / DeepSeek reviewer).
2. **Or look at az42 as-is**: 1.97 is 8% under 3d's defect; if your eye
   accepts it, the chain can close and push.
3. **Or revert the manifest flip** (one line back to
   `env_studio-softbox.png`) if you reject the v2 look; the generator
   profile and tests stay either way (byte-identity of accepted assets is
   untouched).

**Owner sign-offs the round needs regardless** (recorded in derivation
section 8, `output/batch3e-env-horizon-derivation.md`):

- LDR headroom: constraint-11 band-local reading violated as measured
  (pre-clip in-band max 1.29997 where v1 already clips; 13,541 band pixels
  newly clipped). Declared interpretation needs your words or a redesign
  with a cap.
- "Worst azimuth step <= half of v1" bar missed (ratios 0.529 / 0.676).
  Test pins measured numbers, asserts strict improvement.
- New elevational edge: worst smoothed gradient rose 18.96 -> 33.19
  byte/deg at az ~259 / el ~28.4 (taper x negfill interaction). Outside
  both probe frames, but a video-orbit azimuth (~bg 79) images it — a rig
  or taper revision should smooth it before the full-orbit claim.

## What the next agent must know

- **Pair protocol (owner instruction 2026-09-22):** worker + adversarial
  reviewer in pairs, never same model family, roles flip each task. Task 1
  (3e) = DeepSeek worker / GLM reviewer; the rig round flips. Plain
  dispatches (no workflow harness); seats write files, return <= 400
  words; reviewer gets ARTIFACT + CONTRACT only; architect re-runs every
  verification.
- **DeepSeek 429 epidemic tonight:** 3 of 5 dispatches died to
  "429 Too Many Requests" mid-task (~160k tokens of truncated work,
  empty diffs). Cheap probes between attempts always succeeded — bursty
  saturation. Mitigation that worked: split the task (derivation-only
  dispatch first, its file survives a late kill), then a LEAN pinned-design
  dispatch with exploration forbidden and file-writes front-loaded.
  Attempt 5 sailed through. seatwrap rows are in
  `C:/Projects/Misc/seatwrap/ledger/returns.jsonl`.
- **Env artifact chain in `output/`** (gitignored, read-only evidence):
  `batch3e-env-horizon-spec.md` DRAFT-2, `batch3e-env-horizon-derivation.md`
  (+ section 8 revision), `batch3e-deriv-review-glm.md`,
  `batch3e-impl-dispatch.md`, `batch3e-impl-lean-dispatch.md`,
  `batch3e-spec-review-glm.md`, `batch3e-impl-review-glm.md`, and the
  probe/curl prompt files. The derivation doc is append-only: sections 1-7
  carry pre-amendment design numbers; section 8 wins where they disagree.
- **Cheap hardening for the next dispatch** (GLM impl review finding 7):
  `main()` can overwrite `ACCEPTED_OUTPUT` with no sha guard — refuse to
  write the accepted PNG unless the existing sha matches.
- **Concurrent session in this worktree:** five+ untracked
  `scripts/*webexport*.py` files (airway/corridor probes, GLB export, a
  render script) belong to ANOTHER workstream, appeared and grew during
  this session. Do not touch, do not commit, do not "clean up". Also
  untracked: `.codex/`, `.claude/` (tool state). Commit with explicit
  paths only.
- Deliberate dirty tree (never sweep in): `AGENTS.md`, `.zcodeignore`,
  `docs/agent-tooling.md` — same as the 3d handoff recorded.
- `probe_sequence.py` writes `composite-prelens.png`/`composite-lens.png`;
  the Modal dispatcher does not run it. For 2-frame probes:
  `probe_sequence.py --run-dir <dir> --expect-frames 2 --no-encode`.

## Standing constraints (unchanged)

- All rendering on Modal, never local (2026-09-20 ruling); USD 30/month
  ceiling; estimates before dispatch, measured seconds after;
  `cloud_authorized` stays hardcoded false.
- No threshold widened without new numbers — the 1.8 gate and
  `measure_background_step` are untouched; 3e's failure is reported, not
  rationalized. "Looks good" is not owner acceptance.
- Material/asset changes need a new source asset + acceptance round; the
  rig round, if you pick it, is exactly that.
- Single upstream `origin`; the `main` worktree at
  `C:/Projects/msp-render-pipeline` has none of this branch.
- Python: `./.venv/Scripts/python.exe`. Blender at
  `C:/Program Files/Blender Foundation/Blender 5.1/blender.exe`. ffmpeg on
  PATH. Explicit `git add <file>…`, never `-A`.
